{% extends 'base.html.twig' %}

{% block title %}Enlace cony i miguel - perfil{% endblock %}

{% block body %}
{% include 'header.html.twig' %}
<div class="container-fluid text-center">
{% if is_granted('ROLE_ADMIN') %}
assas
{% elseif is_granted('ROLE_YES')  %}
<div class="card text-white bg-dark">
  <h5 class="card-header">Cuenta de banco</h5>
  <div class="card-body">
    <h5 class="card-title">IBAN ES77 1465 0100 9617 4820 4664</h5>
  </div>
</div>
{% elseif is_granted('ROLE_NO')  %}
<p class="text-center fs-4">Se a ido a lo grande en {{ app.user.userIdentifier }} Muchas gracias</p>
<a class="btn btn-danger" href="{{ path('app_logout') }}">Cerrar sesión</a>
{% elseif is_granted('ROLE_USER')  %}
<p class="text-center fs-2"> Queremos que nos acompañes el día de nuestra boda.</p>
<p class="text-center fs-2"> Te agradecemos que confirmes tu asistencia</p>
<a class="btn btn-success" href="{{ path('app_profile_persona', {'id': user.id}) }}">Si asisto</a>
<a class="btn btn-danger" href="{{ path('app_profile_no_attend', {'id': user.id}) }}">No asisto</a>
{% endif %}
</div>
</div>
{% endblock %}
