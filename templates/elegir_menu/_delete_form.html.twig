<form method="post" action="{{ path('app_elegirmenu_delete', {'id': elegir_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ elegir_menu.id) }}">
	<button class="btn">Delete</button>
</form>
